/* 시간 사람이 읽을수 있는 형식으로 변환 해주는 함수 */
export function dateToLocal(date) {
  //   console.log(date.toLocaleString().slice(0, 11));
  const timeOptions = { hour: "2-digit", minute: "2-digit", second: "2-digit" };
}
